<!DOCTYPE html>
<html>
  <head>
      <title>Fractalz</title>
      <meta charset="utf-8">
      <script src="./src/renderer/assets/js/jquery.min.js"></script>
      <script src="./src/renderer/assets/js/popper.min.js"></script>
      <link href="./src/renderer/assets/css/bootstrap.min.css" rel="stylesheet" />
      <link href="./src/renderer/assets/fonts/bootstrap-icons.css" rel="stylesheet" />
      <link href="./src/renderer/assets/css/custom.css" rel="stylesheet" />
      <link href="./src/renderer/assets/css/todoList.css" rel="stylesheet" />
      <script src="./src/renderer/assets/js/bootstrap.min.js"></script>
    <meta charset="utf-8">
    <% if (htmlWebpackPlugin.options.nodeModules) { %>
      <!-- Add `node_modules/` to global paths so `require` works properly in development -->
      <script>
        require('module').globalPaths.push('<%= htmlWebpackPlugin.options.nodeModules.replace(/\\/g, '\\\\') %>')
      </script>
    <% } %>
  </head>
  <body style="margin: 0px; padding: 0px; overflow-x: hidden">
    <div id="app"></div>
    <!-- Set `__static` path to static files in production -->
    <% if (!process.browser) { %>
      <script>
        if (process.env.NODE_ENV !== 'development') window.__static = require('path').join(__dirname, '/static').replace(/\\/g, '\\\\')
      </script>
    <% } %>

    <!-- webpack builds are automatically injected -->
  </body>
</html>
